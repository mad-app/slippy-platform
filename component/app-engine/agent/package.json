{
  "name": "agent",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "watch": "tsc -w --pretty",
    "start": "node .build/main.js",
    "build": "rm -rf node_modules && npm i && rm -rf .build && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/express-fileupload": "^0.4.0",
    "express": "^4.16.4",
    "express-fileupload": "^1.1.3-alpha.1",
    "kafka-node": "^4.0.2",
    "unzipper": "^0.9.11"
  },
  "devDependencies": {
    "@types/unzipper": "^0.9.1"
  }
}
